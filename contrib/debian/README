This directory contains files used to package rurcd/rurc-qt
for Debian-based Linux systems.

If you compile rurcd/rurc-qt yourself, there are some
useful files here:

rurc: URI support
--------------------

rurc-qt.desktop  (Gnome / Open Desktop)
To install:
  sudo desktop-file-install rurc-qt.desktop
  sudo update-desktop-database

If you build yourself, you will either need to modify the paths in
the .desktop file or copy or symlink your rurc-qt binary to /usr/bin
and the ../../share/pixmaps/rurc128.png to /usr/share/pixmaps

rurc-qt.protocol (KDE)

